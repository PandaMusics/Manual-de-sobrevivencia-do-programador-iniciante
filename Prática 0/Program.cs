using System; // Referência a biblioteca padrão

class Prática0 // Classe do programa
{
    private static void Main(string[] args) // Será explicado depois no livro!
    {
        Console.WriteLine("Escreva o que quiser aqui!"); // Comando da biblioteca (System) que escreve algo no console
                                                         // É usado para testar códigos e achar errors ou bugs

        Console.ReadKey(); // Comando da biblioteca (System) que espera o usuário apertar qualquer tecla do seu teclado
    }
}